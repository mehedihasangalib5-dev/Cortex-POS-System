{
  "name": "cortex-payment-functions",
  "description": "Secure server-side piece for Cortex billing: talks to SSLCommerz with the store secret (which must never live in client JS) and is the only writer allowed to change a user's `plan` in Firestore.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  },
  "private": true
}
